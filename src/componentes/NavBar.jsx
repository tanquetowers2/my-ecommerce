import React from "react";
import CartWidget  from "./CartWidget";

const NavBar = () => {
   return (
        <div>
            NavBar
            <CartWidget/>
    
         </div>
    );

};

export default NavBar